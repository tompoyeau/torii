{
  "name": "torii-api",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "schema": "wrangler d1 execute torii --remote --file=schema.sql",
    "schema:local": "wrangler d1 execute torii --local --file=schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
